# URL reachable from the Next.js server only. Do not prefix this with NEXT_PUBLIC_.
# Local development: http://127.0.0.1:8787
COMPILER_SERVICE_URL=http://127.0.0.1:8787

# Set this to the same long random value used by the compiler container in production.
# Optional only for local development.
COMPILER_SERVICE_TOKEN=
