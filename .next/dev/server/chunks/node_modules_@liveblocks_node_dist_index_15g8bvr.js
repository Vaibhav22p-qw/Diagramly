module.exports = [
"[project]/node_modules/@liveblocks/node/dist/index.js [app-route] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "Liveblocks",
    ()=>Liveblocks,
    "LiveblocksError",
    ()=>LiveblocksError,
    "WebhookHandler",
    ()=>WebhookHandler,
    "isCustomNotificationEvent",
    ()=>isCustomNotificationEvent,
    "isTextMentionNotificationEvent",
    ()=>isTextMentionNotificationEvent,
    "isThreadNotificationEvent",
    ()=>isThreadNotificationEvent,
    "markdownToCommentBody",
    ()=>markdownToCommentBody
]);
// src/index.ts
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@liveblocks/core/dist/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$marked$2f$lib$2f$marked$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/marked/lib/marked.esm.js [app-route] (ecmascript)");
// src/webhooks.ts
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stablelib$2f$base64$2f$lib$2f$base64$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stablelib/base64/lib/base64.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$fast$2d$sha256$2f$sha256$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/fast-sha256/sha256.js [app-route] (ecmascript)");
;
// src/version.ts
var PKG_NAME = "@liveblocks/node";
var PKG_VERSION = "3.23.0";
var PKG_FORMAT = "esm";
;
// src/lib/itertools.ts
async function asyncConsume(iterable) {
    const result = [];
    for await (const item of iterable){
        result.push(item);
    }
    return result;
}
async function runConcurrently(iterable, fn, concurrency) {
    const queue = /* @__PURE__ */ new Set();
    for await (const item of iterable){
        if (queue.size >= concurrency) {
            await Promise.race(queue);
        }
        const promise = (async ()=>{
            try {
                await fn(item);
            } finally{
                queue.delete(promise);
            }
        })();
        queue.add(promise);
    }
    if (queue.size > 0) {
        await Promise.all(queue);
    }
}
// src/lib/ndjson.ts
var LineStream = class extends TransformStream {
    constructor(){
        let buffer = "";
        super({
            transform (chunk2, controller) {
                buffer += chunk2;
                if (buffer.includes("\n")) {
                    const lines = buffer.split("\n");
                    for(let i = 0; i < lines.length - 1; i++){
                        if (lines[i].length > 0) {
                            controller.enqueue(lines[i]);
                        }
                    }
                    buffer = lines[lines.length - 1];
                }
            },
            flush (controller) {
                if (buffer.length > 0) {
                    controller.enqueue(buffer);
                }
            }
        });
    }
};
var NdJsonStream = class extends TransformStream {
    constructor(){
        super({
            transform (line, controller) {
                const json = JSON.parse(line);
                controller.enqueue(json);
            }
        });
    }
};
// src/lib/xwarn.ts
function xwarn(resp, method, path) {
    const message = resp.headers.get("X-LB-Warn");
    if (message) {
        const msg = `  \u26A0 [Liveblocks] ${message} (${method} ${path})`;
        if (resp.ok) {
            console.warn(msg);
        } else {
            console.error(msg);
        }
    }
}
;
// src/utils.ts
var DEFAULT_BASE_URL = "https://api.liveblocks.io";
var VALID_KEY_CHARS_REGEX = /^[\w-]+$/;
function getBaseUrl(baseUrl) {
    if (typeof baseUrl === "string" && baseUrl.startsWith("http")) {
        return baseUrl;
    } else {
        return DEFAULT_BASE_URL;
    }
}
async function fetchPolyfill() {
    return typeof globalThis.fetch !== "undefined" ? globalThis.fetch : (await __turbopack_context__.A("[project]/node_modules/node-fetch/lib/index.mjs [app-route] (ecmascript, async loader)")).default;
}
function isString(value) {
    return typeof value === "string";
}
function startsWith(value, prefix) {
    return isString(value) && value.startsWith(prefix);
}
function isNonEmpty(value) {
    return isString(value) && value.length > 0;
}
function assertNonEmpty(value, field) {
    if (!isNonEmpty(value)) {
        throw new Error(`Invalid value for field '${field}'. Please provide a non-empty string. For more information: https://liveblocks.io/docs/api-reference/liveblocks-node#authorize`);
    }
}
function assertSecretKey(value, field) {
    if (!startsWith(value, "sk_")) {
        throw new Error(`Invalid value for field '${field}'. Secret keys must start with 'sk_'. Please provide the secret key from your Liveblocks dashboard at https://liveblocks.io/dashboard/apikeys.`);
    }
    if (!VALID_KEY_CHARS_REGEX.test(value)) {
        throw new Error(`Invalid chars found in field '${field}'. Please check that you correctly copied the secret key from your Liveblocks dashboard at https://liveblocks.io/dashboard/apikeys.`);
    }
}
function normalizeStatusCode(statusCode) {
    if (statusCode >= 200 && statusCode < 300) {
        return 200;
    } else if (statusCode >= 500) {
        return 503;
    } else {
        return statusCode;
    }
}
// src/Session.ts
var MAX_PERMS_PER_SET = 10;
var READ_ACCESS = Object.freeze([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Permission"].Read
]);
var FULL_ACCESS = Object.freeze([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Permission"].Write
]);
var roomPatternRegex = /^([*]|[^*]{1,128}[*]?)$/;
var Session = class {
    /**
   * @deprecated Use `["*:write"]` instead.
   */ FULL_ACCESS = FULL_ACCESS;
    /**
   * @deprecated Use `["*:read"]` instead.
   */ READ_ACCESS = READ_ACCESS;
    #postFn;
    #userId;
    #userInfo;
    #organizationId;
    /** Only used as a hint to produce better error messages. */ #localDev;
    #sealed = false;
    #permissions = /* @__PURE__ */ new Map();
    /** @internal */ constructor(postFn, userId, userInfo, organizationId, localDev){
        assertNonEmpty(userId, "userId");
        this.#postFn = postFn;
        this.#userId = userId;
        this.#userInfo = userInfo;
        this.#organizationId = organizationId;
        this.#localDev = localDev ?? false;
    }
    #getOrCreate(roomId) {
        if (this.#sealed) {
            throw new Error("You can no longer change these permissions.");
        }
        let perms = this.#permissions.get(roomId);
        if (perms) {
            return perms;
        } else {
            if (this.#permissions.size >= MAX_PERMS_PER_SET) {
                throw new Error("You cannot add permissions for more than 10 rooms in a single token");
            }
            perms = /* @__PURE__ */ new Set();
            this.#permissions.set(roomId, perms);
            return perms;
        }
    }
    allow(roomIdOrPattern, newPerms) {
        if (typeof roomIdOrPattern !== "string") {
            throw new Error("Room name or pattern must be a string");
        }
        if (!roomPatternRegex.test(roomIdOrPattern)) {
            throw new Error("Invalid room name or pattern");
        }
        if (newPerms.length === 0) {
            throw new Error("Permission list cannot be empty");
        }
        const permissions = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeRoomPermissions"])(newPerms);
        const existingPerms = this.#getOrCreate(roomIdOrPattern);
        for (const perm of permissions){
            existingPerms.add(perm);
        }
        return this;
    }
    /** @internal - For unit tests only */ hasPermissions() {
        return this.#permissions.size > 0;
    }
    /** @internal - For unit tests only */ seal() {
        if (this.#sealed) {
            throw new Error("You cannot reuse Session instances. Please create a new session every time.");
        }
        this.#sealed = true;
    }
    /** @internal - For unit tests only */ serializePermissions() {
        return Object.fromEntries(Array.from(this.#permissions.entries()).map(([pat, perms])=>[
                pat,
                Array.from(perms)
            ]));
    }
    /**
   * Call this to authorize the session to access Liveblocks. Note that this
   * will return a Liveblocks "access token". Anyone that obtains such access
   * token will have access to the allowed resources.
   */ async authorize() {
        this.seal();
        if (!this.hasPermissions()) {
            console.warn("Access tokens without any permission will not be supported soon, you should use wildcards when the client requests a token for resources outside a room. See https://liveblocks.io/docs/errors/liveblocks-client/access-tokens-not-enough-permissions");
        }
        try {
            const body = {
                // Required
                userId: this.#userId,
                permissions: this.serializePermissions(),
                // Optional metadata
                userInfo: this.#userInfo
            };
            if (this.#organizationId !== void 0) {
                body.organizationId = this.#organizationId;
            }
            const resp = await this.#postFn(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/authorize-user`, body);
            return {
                status: normalizeStatusCode(resp.status),
                body: await resp.text()
            };
        } catch (er) {
            return {
                status: 503,
                body: this.#localDev ? "Could not connect to your Liveblocks dev server. Is it running?" : 'Call to /v2/authorize-user failed. See "error" for more information.',
                error: er
            };
        }
    }
};
// src/client.ts
var ROOM_FILE_PART_SIZE = 5 * 1024 * 1024;
var ROOM_FILE_RETRY_ATTEMPTS = 10;
var ROOM_FILE_RETRY_DELAYS = [
    2e3,
    2e3,
    2e3,
    2e3,
    2e3,
    2e3,
    2e3,
    2e3,
    2e3,
    2e3
];
async function uploadRoomFile({ file, signal, abortErrorMessage, retryMultipartCompletion, uploadSingle, createMultipartUpload, uploadMultipartPart, completeMultipartUpload, abortMultipartUpload }) {
    const abortError = createAbortError(abortErrorMessage);
    if (signal?.aborted) {
        throw abortError;
    }
    const handleRetryError = (err)=>{
        if (signal?.aborted) {
            throw abortError;
        }
        return err instanceof LiveblocksError && err.status >= 400 && err.status < 500;
    };
    if (file.size <= ROOM_FILE_PART_SIZE) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["autoRetry"])(uploadSingle, ROOM_FILE_RETRY_ATTEMPTS, ROOM_FILE_RETRY_DELAYS, handleRetryError);
    }
    let uploadId;
    const uploadedParts = [];
    const multipartUpload = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["autoRetry"])(createMultipartUpload, ROOM_FILE_RETRY_ATTEMPTS, ROOM_FILE_RETRY_DELAYS, handleRetryError);
    const partUploadController = new AbortController();
    const partUploadSignal = partUploadController.signal;
    const abortPartUploads = (reason)=>{
        partUploadController.abort(reason);
    };
    const handleExternalAbort = ()=>abortPartUploads();
    if (signal?.aborted) {
        handleExternalAbort();
    } else {
        signal?.addEventListener("abort", handleExternalAbort, {
            once: true
        });
    }
    try {
        uploadId = multipartUpload.uploadId;
        if (signal?.aborted) {
            throw abortError;
        }
        const batches = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["chunk"])(splitRoomFileIntoParts(file), 5);
        for (const parts of batches){
            const firstPartUploadFailure = {};
            const partUploads$ = [];
            for (const { part, partNumber } of parts){
                partUploads$.push((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["autoRetry"])(()=>uploadMultipartPart(multipartUpload.uploadId, partNumber, part, partUploadSignal), ROOM_FILE_RETRY_ATTEMPTS, ROOM_FILE_RETRY_DELAYS, (error)=>{
                    if (signal?.aborted) {
                        throw abortError;
                    }
                    return partUploadSignal.aborted || handleRetryError(error);
                }).catch((error)=>{
                    if (firstPartUploadFailure.value === void 0) {
                        firstPartUploadFailure.value = {
                            error
                        };
                        abortPartUploads(error);
                    }
                    throw error;
                }));
            }
            const settledPartUploads = await Promise.allSettled(partUploads$);
            if (firstPartUploadFailure.value !== void 0) {
                throw firstPartUploadFailure.value.error;
            }
            for (const settledPartUpload of settledPartUploads){
                if (settledPartUpload.status === "fulfilled") {
                    uploadedParts.push(settledPartUpload.value);
                }
            }
        }
        if (signal?.aborted) {
            throw abortError;
        }
        const sortedParts = uploadedParts.sort((a, b)=>a.partNumber - b.partNumber);
        return retryMultipartCompletion ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["autoRetry"])(()=>completeMultipartUpload(multipartUpload.uploadId, sortedParts), ROOM_FILE_RETRY_ATTEMPTS, ROOM_FILE_RETRY_DELAYS, handleRetryError) : completeMultipartUpload(uploadId, sortedParts);
    } catch (err) {
        if (uploadId) {
            try {
                await abortMultipartUpload(uploadId);
            } catch  {}
        }
        throw err;
    } finally{
        signal?.removeEventListener("abort", handleExternalAbort);
    }
}
function splitRoomFileIntoParts(file) {
    const parts = [];
    let start = 0;
    while(start < file.size){
        const end = Math.min(start + ROOM_FILE_PART_SIZE, file.size);
        parts.push({
            partNumber: parts.length + 1,
            part: file.slice(start, end)
        });
        start = end;
    }
    return parts;
}
function createAbortError(message) {
    if (typeof DOMException === "function") {
        return new DOMException(message, "AbortError");
    }
    const err = new Error(message);
    err.name = "AbortError";
    return err;
}
function inflateRoomData(room) {
    const createdAt = new Date(room.createdAt);
    const lastConnectionAt = room.lastConnectionAt ? new Date(room.lastConnectionAt) : void 0;
    return {
        ...room,
        createdAt,
        lastConnectionAt
    };
}
function normalizeCreateRoomOptions(options) {
    return {
        ...options,
        defaultAccesses: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeRoomPermissions"])(options.defaultAccesses),
        groupsAccesses: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeRoomAccesses"])(options.groupsAccesses),
        usersAccesses: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeRoomAccesses"])(options.usersAccesses)
    };
}
function normalizeUpdateRoomOptions(options) {
    return {
        ...options,
        defaultAccesses: options.defaultAccesses === void 0 || options.defaultAccesses === null ? options.defaultAccesses : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeRoomPermissions"])(options.defaultAccesses),
        groupsAccesses: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeUpdateRoomAccesses"])(options.groupsAccesses),
        usersAccesses: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeUpdateRoomAccesses"])(options.usersAccesses)
    };
}
function inflateAiCopilot(copilot) {
    return {
        ...copilot,
        createdAt: new Date(copilot.createdAt),
        updatedAt: new Date(copilot.updatedAt),
        lastUsedAt: copilot.lastUsedAt ? new Date(copilot.lastUsedAt) : void 0
    };
}
function inflateKnowledgeSource(source) {
    return {
        ...source,
        createdAt: new Date(source.createdAt),
        updatedAt: new Date(source.updatedAt),
        lastIndexedAt: new Date(source.lastIndexedAt)
    };
}
function inflateWebKnowledgeSourceLink(link) {
    return {
        ...link,
        createdAt: new Date(link.createdAt),
        lastIndexedAt: new Date(link.lastIndexedAt)
    };
}
function inflateHistoryVersion(version) {
    return {
        ...version,
        createdAt: new Date(version.createdAt)
    };
}
var Liveblocks = class {
    #secret;
    #baseUrl;
    /** Only used as a hint to produce better error messages. */ #localDev;
    /**
   * Interact with the Liveblocks API from your Node.js backend.
   */ constructor(options){
        const options_ = options;
        const secret = options_.secret;
        assertSecretKey(secret, "secret");
        this.#secret = secret;
        this.#baseUrl = new URL(getBaseUrl(options.baseUrl));
        this.#localDev = !!options.baseUrl && /^https?:\/\/localhost[:/]/.test(options.baseUrl);
    }
    async #post(path, json, options, params) {
        const url3 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, path, params);
        const headers = {
            Authorization: `Bearer ${this.#secret}`,
            "Content-Type": "application/json"
        };
        const fetch = await fetchPolyfill();
        const res = await fetch(url3, {
            method: "POST",
            headers,
            body: JSON.stringify(json),
            signal: options?.signal
        });
        xwarn(res, "POST", path);
        return res;
    }
    async #readJsonResponse(res) {
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    async #patch(path, json, options) {
        const url3 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, path);
        const headers = {
            Authorization: `Bearer ${this.#secret}`,
            "Content-Type": "application/json"
        };
        const fetch = await fetchPolyfill();
        const res = await fetch(url3, {
            method: "PATCH",
            headers,
            body: JSON.stringify(json),
            signal: options?.signal
        });
        xwarn(res, "PATCH", path);
        return res;
    }
    async #putBinary(path, body, params, options) {
        const url3 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, path, params);
        const headers = {
            Authorization: `Bearer ${this.#secret}`,
            "Content-Type": "application/octet-stream"
        };
        const fetch = await fetchPolyfill();
        const res = await fetch(url3, {
            method: "PUT",
            headers,
            body,
            signal: options?.signal
        });
        xwarn(res, "PUT", path);
        return res;
    }
    async #putBlob(path, body, params, options) {
        const url3 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, path, params);
        const headers = {
            Authorization: `Bearer ${this.#secret}`,
            "Content-Type": "application/octet-stream"
        };
        const fetch = await fetchPolyfill();
        const res = await fetch(url3, {
            method: "PUT",
            headers,
            body,
            signal: options?.signal
        });
        xwarn(res, "PUT", path);
        return res;
    }
    async #delete(path, params, options) {
        const url3 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, path, params);
        const headers = {
            Authorization: `Bearer ${this.#secret}`
        };
        const fetch = await fetchPolyfill();
        const res = await fetch(url3, {
            method: "DELETE",
            headers,
            signal: options?.signal
        });
        xwarn(res, "DELETE", path);
        return res;
    }
    async #get(path, params, options) {
        const url3 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, path, params);
        const headers = {
            Authorization: `Bearer ${this.#secret}`
        };
        const fetch = await fetchPolyfill();
        const res = await fetch(url3, {
            method: "GET",
            headers,
            signal: options?.signal
        });
        xwarn(res, "GET", path);
        return res;
    }
    /* -------------------------------------------------------------------------------------------------
   * Authentication
   * -----------------------------------------------------------------------------------------------*/ /**
   * Prepares a new session to authorize a user to access Liveblocks.
   *
   * IMPORTANT:
   * Always make sure that you trust the user making the request to your
   * backend before calling .prepareSession()!
   *
   * @param userId Tell Liveblocks the user ID of the user to authorize. Must
   * uniquely identify the user account in your system. The uniqueness of this
   * value will determine how many MAUs will be counted/billed.
   *
   * @param options.organizationId (optional) The organization ID to authorize the user for.
   *
   * @param options.userInfo Custom metadata to attach to this user. Data you
   * add here will be visible to all other clients in the room, through the
   * `other.info` property.
   *
   */ prepareSession(userId, ...rest) {
        const options = rest[0];
        return new Session(this.#post.bind(this), userId, options?.userInfo, options?.organizationId ?? options?.tenantId, this.#localDev);
    }
    /**
   * Call this to authenticate the user as an actor you want to allow to use
   * Liveblocks.
   *
   * You should use this method only if you want to manage your permissions
   * through the Liveblocks Permissions API. This method is more complicated to
   * set up, but allows for finer-grained specification of permissions.
   *
   * Calling `.identifyUser()` only lets you securely identify a user (and what
   * groups they belong to). What permissions this user will end up having is
   * determined by whatever permissions you assign the user/group in your
   * Liveblocks account, through the Permissions API:
   * https://liveblocks.io/docs/rooms/permissions
   *
   * IMPORTANT:
   * Always verify that you trust the user making the request before calling
   * .identifyUser()!
   *
   * @param identity Tell Liveblocks the user ID of the user to authenticate.
   * Must uniquely identify the user account in your system. The uniqueness of
   * this value will determine how many MAUs will be counted/billed.
   *
   * If you also want to assign which groups this user belongs to, use the
   * object form and specify the `groupIds` property. Those `groupIds` should
   * match the groupIds you assigned permissions to via the Liveblocks
   * Permissions API, see
   * https://liveblocks.io/docs/rooms/permissions#permissions-levels-groups-accesses-example
   *
   * @param options.userInfo Custom metadata to attach to this user. Data you
   * add here will be visible to all other clients in the room, through the
   * `other.info` property.
   */ // These fields define the security identity of the user. Whatever you pass in here will define which
    async identifyUser(identity, ...rest) {
        const options = rest[0];
        const path = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/identify-user`;
        const { userId, groupIds, tenantId, organizationId } = typeof identity === "string" ? {
            userId: identity,
            groupIds: void 0,
            tenantId: void 0,
            organizationId: void 0
        } : identity;
        assertNonEmpty(userId, "userId");
        const body = {
            userId,
            groupIds,
            userInfo: options?.userInfo
        };
        if (organizationId !== void 0) {
            body.organizationId = organizationId;
        } else if (tenantId !== void 0) {
            body.organizationId = tenantId;
        }
        try {
            const resp = await this.#post(path, body);
            return {
                status: normalizeStatusCode(resp.status),
                body: await resp.text()
            };
        } catch (er) {
            return {
                status: 503,
                body: this.#localDev ? "Could not connect to your Liveblocks dev server. Is it running?" : `Call to ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, path)} failed. See "error" for more information.`,
                error: er
            };
        }
    }
    /* -------------------------------------------------------------------------------------------------
   * Room
   * -----------------------------------------------------------------------------------------------*/ /**
   * Returns a list of your rooms. The rooms are returned sorted by creation date, from newest to oldest. You can filter rooms by metadata, users accesses and groups accesses.
   * @param params.limit (optional) A limit on the number of rooms to be returned. The limit can range between 1 and 100, and defaults to 20.
   * @param params.startingAfter (optional) A cursor used for pagination. You get the value from the response of the previous page.
   * @param params.userId (optional) A filter on users accesses.
   * @param params.metadata (optional) A filter on metadata. Multiple metadata keys can be used to filter rooms.
   * @param params.groupIds (optional) A filter on groups accesses. Multiple groups can be used.
   * @param params.organizationId (optional) A filter on organization ID.
   * @param params.query (optional) A query to filter rooms by. It is based on our query language. You can filter by metadata and room ID.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A list of rooms.
   */ async getRooms(params = {}, options) {
        const path = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms`;
        let query;
        if (typeof params.query === "string") {
            query = params.query;
        } else if (typeof params.query === "object") {
            query = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["objectToQuery"])(params.query);
        }
        const queryParams = {
            limit: params.limit,
            startingAfter: params.startingAfter,
            userId: params.userId,
            groupIds: params.groupIds ? params.groupIds.join(",") : void 0,
            query
        };
        if (params.organizationId !== void 0) {
            queryParams.organizationId = params.organizationId;
        } else if (params.tenantId !== void 0) {
            queryParams.organizationId = params.tenantId;
        }
        const res = await this.#get(path, queryParams, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        const rooms = page.data.map(inflateRoomData);
        return {
            ...page,
            data: rooms
        };
    }
    /**
   * Iterates over all rooms that match the given criteria.
   *
   * The difference with .getRooms() is that pagination will happen
   * automatically under the hood, using the given `pageSize`.
   *
   * @param criteria.userId (optional) A filter on users accesses.
   * @param criteria.groupIds (optional) A filter on groups accesses. Multiple groups can be used.
   * @param criteria.query.roomId (optional) A filter by room ID.
   * @param criteria.query.metadata (optional) A filter by metadata.
   *
   * @param options.pageSize (optional) The page size to use for each request.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async *iterRooms(criteria, options) {
        const { signal } = options ?? {};
        const pageSize = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkBounds"])("pageSize", options?.pageSize ?? 40, 20);
        let cursor = void 0;
        while(true){
            const { nextCursor, data } = await this.getRooms({
                ...criteria,
                startingAfter: cursor,
                limit: pageSize
            }, {
                signal
            });
            for (const item of data){
                yield item;
            }
            if (!nextCursor) {
                break;
            }
            cursor = nextCursor;
        }
    }
    /**
   * Creates a new room with the given id.
   * @param roomId The id of the room to create.
   * @param params.defaultAccesses The default accesses for the room.
   * @param params.groupsAccesses (optional) The group accesses for the room. Can contain a maximum of 100 entries. Key length has a limit of 40 characters.
   * @param params.usersAccesses (optional) The user accesses for the room. Can contain a maximum of 100 entries. Key length has a limit of 40 characters.
   * @param params.metadata (optional) The metadata for the room. Supports upto a maximum of 50 entries. Key length has a limit of 40 characters. Value length has a limit of 256 characters.
   * @param params.organizationId (optional) The organization ID to create the room for.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The created room.
   */ async createRoom(roomId, params, options) {
        const normalizedParams = normalizeCreateRoomOptions(params);
        const { defaultAccesses, groupsAccesses, usersAccesses, metadata, tenantId, organizationId } = normalizedParams;
        const body = {
            id: roomId,
            defaultAccesses,
            groupsAccesses,
            usersAccesses,
            metadata
        };
        if (organizationId !== void 0) {
            body.organizationId = organizationId;
        } else if (tenantId !== void 0) {
            body.organizationId = tenantId;
        }
        const res = await this.#post(options?.idempotent ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms?idempotent` : __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms`, body, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateRoomData(data);
    }
    /**
   * Returns a room with the given id, or creates one with the given creation
   * options if it doesn't exist yet.
   *
   * @param roomId The id of the room.
   * @param params.defaultAccesses The default accesses for the room if the room will be created.
   * @param params.groupsAccesses (optional) The group accesses for the room if the room will be created. Can contain a maximum of 100 entries. Key length has a limit of 40 characters.
   * @param params.usersAccesses (optional) The user accesses for the room if the room will be created. Can contain a maximum of 100 entries. Key length has a limit of 40 characters.
   * @param params.metadata (optional) The metadata for the room if the room will be created. Supports upto a maximum of 50 entries. Key length has a limit of 40 characters. Value length has a limit of 256 characters.
   * @param params.organizationId (optional) The organization ID to create the room for.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The room.
   */ async getOrCreateRoom(roomId, params, options) {
        return await this.createRoom(roomId, params, {
            ...options,
            idempotent: true
        });
    }
    /**
   * Updates or creates a new room with the given properties.
   *
   * @param roomId The id of the room to update or create.
   * @param update The fields to update. These values will be updated when the room exists, or set when the room does not exist and gets created. Must specify at least one key.
   * @param create (optional) The fields to only use when the room does not exist and will be created. When the room already exists, these values are ignored.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The room.
   */ async upsertRoom(roomId, params, options) {
        const body = {
            update: normalizeUpdateRoomOptions(params.update),
            create: params.create === void 0 ? void 0 : normalizeCreateRoomOptions(params.create)
        };
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/upsert`, body, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateRoomData(data);
    }
    /**
   * Returns a room with the given id.
   * @param roomId The id of the room to return.
   * @returns The room with the given id.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getRoom(roomId, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateRoomData(data);
    }
    /**
   * Updates specific properties of a room. It’s not necessary to provide the entire room’s information.
   * Setting a property to `null` means to delete this property.
   * @param roomId The id of the room to update.
   * @param params.defaultAccesses (optional) The default accesses for the room.
   * @param params.groupsAccesses (optional) The group accesses for the room. Can contain a maximum of 100 entries. Key length has a limit of 40 characters.
   * @param params.usersAccesses (optional) The user accesses for the room. Can contain a maximum of 100 entries. Key length has a limit of 40 characters.
   * @param params.metadata (optional) The metadata for the room. Supports upto a maximum of 50 entries. Key length has a limit of 40 characters. Value length has a limit of 256 characters.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The updated room.
   */ async updateRoom(roomId, params, options) {
        const { defaultAccesses, groupsAccesses, usersAccesses, metadata } = normalizeUpdateRoomOptions(params);
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}`, {
            defaultAccesses,
            groupsAccesses,
            usersAccesses,
            metadata
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateRoomData(data);
    }
    /**
   * Deletes a room with the given id. A deleted room is no longer accessible from the API or the dashboard and it cannot be restored.
   * @param roomId The id of the room to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteRoom(roomId, options) {
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Prepares a room for connectivity, making the eventual connection faster. Use this when you know you'll be loading a room but are not yet connected to it.
   * @param roomId The id of the room to prewarm.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async prewarmRoom(roomId, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/prewarm`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Returns a list of users currently present in the requested room. For better performance, we recommand to call this endpoint every 10 seconds maximum. Duplicates can happen if a user is in the requested room with multiple browser tabs opened.
   * @param roomId The id of the room to get the users from.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A list of users currently present in the requested room.
   */ async getActiveUsers(roomId, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/active-users`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Boadcasts an event to a room without having to connect to it via the client from @liveblocks/client. The connectionId passed to event listeners is -1 when using this API.
   * @param roomId The id of the room to broadcast the event to.
   * @param message The message to broadcast. It can be any JSON serializable value.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async broadcastEvent(roomId, message, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/broadcast-event`, message, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Sets ephemeral presence for a user in a room without requiring a WebSocket connection.
   * The presence data will automatically expire after the specified TTL.
   * This is useful for scenarios like showing an AI agent's presence in a room.
   *
   * @param roomId The id of the room to set presence in.
   * @param params.userId The ID of the user to set presence for.
   * @param params.data The presence data as a JSON object.
   * @param params.userInfo (optional) Metadata about the user or agent
   * @param params.ttl (optional) Time-to-live in seconds. If not specified, the default TTL is 60 seconds. (minimum: 2, maximum: 3599).
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async setPresence(roomId, params, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/presence`, {
            userId: params.userId,
            data: params.data,
            userInfo: params.userInfo,
            ttl: params.ttl
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    async getStorageDocument(roomId, format = "plain-lson", options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage`, {
            format
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    async #requestStorageMutation(roomId, options) {
        const resp = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/request-storage-mutation`, {}, options);
        if (!resp.ok) {
            throw await LiveblocksError.from(resp);
        }
        if (resp.headers.get("content-type") !== "application/x-ndjson") {
            throw new Error("Unexpected response content type");
        }
        if (resp.body === null) {
            throw new Error("Unexpected null body in response");
        }
        const stream = resp.body.pipeThrough(new TextDecoderStream()).pipeThrough(new LineStream()).pipeThrough(new NdJsonStream());
        const iter = stream[Symbol.asyncIterator]();
        const first = (await iter.next()).value;
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isPlainObject"])(first) || typeof first.actor !== "number") {
            throw new Error("Failed to obtain a unique session");
        }
        const nodes = await asyncConsume(iter);
        return {
            actor: first.actor,
            nodes
        };
    }
    /**
   * Initializes a room’s Storage. The room must already exist and have an empty Storage.
   * Calling this endpoint will disconnect all users from the room if there are any.
   *
   * @param roomId The id of the room to initialize the storage from.
   * @param document The document to initialize the storage with.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The initialized storage document. It is of the same format as the one passed in.
   */ async initializeStorageDocument(roomId, document, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage`, document, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Deletes all of the room’s Storage data and disconnect all users from the room if there are any. Note that this does not delete the Yjs document in the room if one exists.
   * @param roomId The id of the room to delete the storage from.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteStorageDocument(roomId, options) {
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /* -------------------------------------------------------------------------------------------------
   * Yjs
   * -----------------------------------------------------------------------------------------------*/ /**
   * Returns a JSON representation of the room’s Yjs document.
   * @param roomId The id of the room to get the Yjs document from.
   * @param params.format (optional) If true, YText will return formatting.
   * @param params.key (optional) If provided, returns only a single key’s value, e.g. doc.get(key).toJSON().
   * @param params.type (optional) Used with key to override the inferred type, i.e. "ymap" will return doc.get(key, Y.Map).
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A JSON representation of the room’s Yjs document.
   */ async getYjsDocument(roomId, params = {}, options) {
        const { format, key, type } = params;
        const path = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`v2/rooms/${roomId}/ydoc`;
        const res = await this.#get(path, {
            formatting: format ? "true" : void 0,
            key,
            type
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Send a Yjs binary update to the room’s Yjs document. You can use this endpoint to initialize Yjs data for the room or to update the room’s Yjs document.
   * @param roomId The id of the room to send the Yjs binary update to.
   * @param update The Yjs update to send. Typically the result of calling `Yjs.encodeStateAsUpdate(doc)`. Read the [Yjs documentation](https://docs.yjs.dev/api/document-updates) to learn how to create a binary update.
   * @param params.guid (optional) If provided, the binary update will be applied to the Yjs subdocument with the given guid. If not provided, the binary update will be applied to the root Yjs document.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async sendYjsBinaryUpdate(roomId, update, params = {}, options) {
        const res = await this.#putBinary(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/ydoc`, update, {
            guid: params.guid
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Returns the room’s Yjs document encoded as a single binary update. This can be used by Y.applyUpdate(responseBody) to get a copy of the document in your backend.
   * See [Yjs documentation](https://docs.yjs.dev/api/document-updates) for more information on working with updates.
   * @param roomId The id of the room to get the Yjs document from.
   * @param params.guid (optional) If provided, returns the binary update of the Yjs subdocument with the given guid. If not provided, returns the binary update of the root Yjs document.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The room’s Yjs document encoded as a single binary update.
   */ async getYjsDocumentAsBinaryUpdate(roomId, params = {}, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/ydoc-binary`, {
            guid: params.guid
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return res.arrayBuffer();
    }
    /**
   * Returns the version history snapshots for the room, sorted by creation date from newest to oldest.
   * @param roomId The ID of the room to get the version history from.
   * @param params.limit (optional) The number of versions to return. The limit can range between 1 and 100, and defaults to 20.
   * @param params.cursor (optional) A cursor used for pagination. Get the value from the `nextCursor` response of the previous page.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A list of version history snapshots and the next page cursor.
   */ async getVersionHistory(roomId, params = {}, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/versions`, {
            limit: params.limit,
            cursor: params.cursor
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        return {
            nextCursor: page.nextCursor,
            data: page.data.map(inflateHistoryVersion)
        };
    }
    /**
   * Creates a new version history snapshot of the room, capturing both its Storage and Yjs documents.
   * @param roomId The ID of the room to create a version history snapshot for.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The created version ID.
   */ async createVersionHistorySnapshot(roomId, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/versions`, {}, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Returns a specific version of the room's Yjs document encoded as a binary Yjs update.
   * @param params.roomId The room ID to get the Yjs document version from.
   * @param params.versionId The ID of the version to get.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The version's Yjs document encoded as a binary update.
   */ async getYjsVersion(params, options) {
        const { roomId, versionId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/versions/${versionId}/yjs`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return res.arrayBuffer();
    }
    /**
   * Permanently deletes a version from the room's history.
   * @param params.roomId The room ID to delete the version in.
   * @param params.versionId The ID of the version to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteVersion(params, options) {
        const { roomId, versionId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/versions/${versionId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /* -------------------------------------------------------------------------------------------------
   * Comments
   * -----------------------------------------------------------------------------------------------*/ /**
   * Gets all the threads in a room.
   *
   * @param params.roomId The room ID to get the threads from.
   * @param params.query The query to filter threads by. It is based on our query language and can filter by visibility, metadata, and resolved status.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A list of threads.
   */ async getThreads(params, options) {
        const { roomId } = params;
        let query;
        if (typeof params.query === "string") {
            query = params.query;
        } else if (typeof params.query === "object") {
            query = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["objectToQuery"])(params.query);
        }
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads`, {
            query
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const { data } = await res.json();
        return {
            data: data.map((thread)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToThreadData"])(thread))
        };
    }
    /**
   * Gets a thread.
   *
   * @param params.roomId The room ID to get the thread from.
   * @param params.threadId The thread ID.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A thread.
   */ async getThread(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToThreadData"])(await res.json());
    }
    /**
   * @deprecated Prefer using `getMentionsFromCommentBody` to extract mentions
   * from comments and threads, or `Liveblocks.getThreadSubscriptions` to get
   * the list of users who are subscribed to a thread.
   *
   * Gets a thread's participants.
   *
   * Participants are users who have commented on the thread
   * or users that have been mentioned in a comment.
   *
   * @param params.roomId The room ID to get the thread participants from.
   * @param params.threadId The thread ID to get the participants from.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns An object containing an array of participant IDs.
   */ async getThreadParticipants(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/participants`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Gets a thread's subscriptions.
   *
   * @param params.roomId The room ID to get the thread subscriptions from.
   * @param params.threadId The thread ID to get the subscriptions from.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns An array of subscriptions.
   */ async getThreadSubscriptions(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/subscriptions`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const { data } = await res.json();
        return {
            data: data.map(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToUserSubscriptionData"])
        };
    }
    /**
   * Gets a thread's comment.
   *
   * @param params.roomId The room ID to get the comment from.
   * @param params.threadId The thread ID to get the comment from.
   * @param params.commentId The comment ID.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A comment.
   */ async getComment(params, options) {
        const { roomId, threadId, commentId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/comments/${commentId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToCommentData"])(await res.json());
    }
    /**
   * Creates a comment.
   *
   * @param params.roomId The room ID to create the comment in.
   * @param params.threadId The thread ID to create the comment in.
   * @param params.data.userId The user ID of the user who is set to create the comment.
   * @param params.data.createdAt (optional) The date the comment is set to be created.
   * @param params.data.body The body of the comment.
   * @param params.data.attachmentIds (optional) The attachment IDs to add to the comment.
   * @param params.data.metadata (optional) The metadata for the comment.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The created comment.
   */ async createComment(params, options) {
        const { roomId, threadId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/comments`, {
            ...data,
            createdAt: data.createdAt?.toISOString()
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToCommentData"])(await res.json());
    }
    /**
   * Edits a comment.
   * @param params.roomId The room ID to edit the comment in.
   * @param params.threadId The thread ID to edit the comment in.
   * @param params.commentId The comment ID to edit.
   * @param params.data.body The body of the comment.
   * @param params.data.attachmentIds (optional) The IDs of every attachment that should remain on the comment.
   * @param params.data.metadata (optional) The metadata for the comment. Value must be a string, boolean or number. Use null to delete a key.
   * @param params.data.editedAt (optional) The date the comment was edited.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The edited comment.
   */ async editComment(params, options) {
        const { roomId, threadId, commentId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/comments/${commentId}`, {
            body: data.body,
            editedAt: data.editedAt?.toISOString(),
            attachmentIds: data.attachmentIds,
            metadata: data.metadata
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToCommentData"])(await res.json());
    }
    /**
   * Deletes a comment. Deletes a comment. If there are no remaining comments in the thread, the thread is also deleted.
   * @param params.roomId The room ID to delete the comment in.
   * @param params.threadId The thread ID to delete the comment in.
   * @param params.commentId The comment ID to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteComment(params, options) {
        const { roomId, threadId, commentId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/comments/${commentId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Gets an attachment's metadata and a presigned download URL.
   *
   * @param params.roomId The room ID the attachment belongs to.
   * @param params.attachmentId The attachment ID (starts with "at_").
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The attachment metadata including a presigned download URL.
   */ async getAttachment(params, options) {
        const { roomId, attachmentId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/attachments/${attachmentId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Uploads an attachment that can be added to a comment.
   *
   * @param params.roomId The room ID to upload the attachment to.
   * @param params.userId The user ID of the user uploading the attachment.
   * @param params.file The file to upload.
   * @param options.signal (optional) An abort signal to cancel the upload.
   * @returns The uploaded attachment.
   */ async uploadAttachment(params, options) {
        const { roomId, userId, file } = params;
        const attachmentId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createCommentAttachmentId"])();
        return await uploadRoomFile({
            file,
            signal: options?.signal,
            abortErrorMessage: `Upload of attachment ${attachmentId} was aborted.`,
            retryMultipartCompletion: false,
            uploadSingle: async ()=>{
                const res = await this.#putBlob(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/attachments/${attachmentId}/upload/${file.name}`, file, {
                    fileSize: file.size,
                    userId
                }, options);
                return await this.#readJsonResponse(res);
            },
            createMultipartUpload: async ()=>{
                const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/attachments/${attachmentId}/multipart/${file.name}`, void 0, options, {
                    fileSize: file.size
                });
                return await this.#readJsonResponse(res);
            },
            uploadMultipartPart: async (uploadId, partNumber, part, signal)=>{
                const res = await this.#putBlob(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/attachments/${attachmentId}/multipart/${uploadId}/${String(partNumber)}`, part, void 0, {
                    signal
                });
                return await this.#readJsonResponse(res);
            },
            completeMultipartUpload: async (uploadId, parts)=>{
                const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/attachments/${attachmentId}/multipart/${uploadId}/complete`, {
                    parts
                }, options, {
                    userId
                });
                return await this.#readJsonResponse(res);
            },
            abortMultipartUpload: async (uploadId)=>{
                const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/attachments/${attachmentId}/multipart/${uploadId}`);
                if (!res.ok) {
                    throw await LiveblocksError.from(res);
                }
            }
        });
    }
    async uploadFile(params, options) {
        const { roomId, file } = params;
        const fileId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createStorageFileId"])();
        const fileData = await uploadRoomFile({
            file,
            signal: options?.signal,
            abortErrorMessage: `Upload of file ${fileId} was aborted.`,
            retryMultipartCompletion: true,
            uploadSingle: async ()=>{
                const res = await this.#putBlob(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage/files/${fileId}/upload/${file.name}`, file, {
                    fileSize: file.size
                }, options);
                return await this.#readJsonResponse(res);
            },
            createMultipartUpload: async ()=>{
                const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage/files/${fileId}/multipart/${file.name}`, void 0, options, {
                    fileSize: file.size
                });
                return await this.#readJsonResponse(res);
            },
            uploadMultipartPart: async (uploadId, partNumber, part, signal)=>{
                const res = await this.#putBlob(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage/files/${fileId}/multipart/${uploadId}/${String(partNumber)}`, part, void 0, {
                    signal
                });
                return await this.#readJsonResponse(res);
            },
            completeMultipartUpload: async (uploadId, parts)=>{
                const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage/files/${fileId}/multipart/${uploadId}/complete`, {
                    parts
                }, options);
                return await this.#readJsonResponse(res);
            },
            abortMultipartUpload: async (uploadId)=>{
                const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage/files/${fileId}/multipart/${uploadId}`);
                if (!res.ok) {
                    throw await LiveblocksError.from(res);
                }
            }
        });
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["LiveFile"](fileData);
    }
    async getFileUrl(params, options) {
        const { roomId, file } = params;
        const fileId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getLiveFileId"])(file);
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/storage/files/${fileId}`, void 0, options);
        const storageFile = await this.#readJsonResponse(res);
        return {
            url: storageFile.url,
            expiresAt: storageFile.expiresAt
        };
    }
    /**
   * Creates a new thread. The thread will be created with the specified comment as its first comment.
   * If the thread already exists, a `LiveblocksError` will be thrown with status code 409.
   * @param params.roomId The room ID to create the thread in.
   * @param params.thread.metadata (optional) The metadata for the thread. Supports upto a maximum of 10 entries. Value must be a string, boolean or number
   * @param params.thread.comment.userId The user ID of the user who created the comment.
   * @param params.thread.comment.createdAt (optional) The date the comment was created.
   * @param params.thread.comment.body The body of the comment.
   * @param params.thread.comment.attachmentIds (optional) The attachment IDs to add to the comment.
   * @param params.thread.comment.metadata (optional) The metadata for the comment.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The created thread. The thread will be created with the specified comment as its first comment.
   */ async createThread(params, options) {
        const { roomId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads`, {
            ...data,
            comment: {
                ...data.comment,
                createdAt: data.comment.createdAt?.toISOString()
            }
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToThreadData"])(await res.json());
    }
    /**
   * Deletes a thread and all of its comments.
   * @param params.roomId The room ID to delete the thread in.
   * @param params.threadId The thread ID to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteThread(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Mark a thread as resolved.
   * @param params.roomId The room ID of the thread.
   * @param params.threadId The thread ID to mark as resolved.
   * @param params.data.userId The user ID of the user who marked the thread as resolved.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The thread marked as resolved.
   */ async markThreadAsResolved(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/mark-as-resolved`, {
            userId: params.data.userId
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToThreadData"])(await res.json());
    }
    /**
   * Mark a thread as unresolved.
   * @param params.roomId The room ID of the thread.
   * @param params.threadId The thread ID to mark as unresolved.
   * @param params.data.userId The user ID of the user who marked the thread as unresolved.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The thread marked as unresolved.
   */ async markThreadAsUnresolved(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/mark-as-unresolved`, {
            userId: params.data.userId
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToThreadData"])(await res.json());
    }
    /**
   * Subscribes a user to a thread.
   * @param params.roomId The room ID of the thread.
   * @param params.threadId The thread ID to subscribe to.
   * @param params.data.userId The user ID of the user to subscribe to the thread.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The thread subscription.
   */ async subscribeToThread(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/subscribe`, {
            userId: params.data.userId
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToSubscriptionData"])(await res.json());
    }
    /**
   * Unsubscribes a user from a thread.
   * @param params.roomId The room ID of the thread.
   * @param params.threadId The thread ID to unsubscribe from.
   * @param params.data.userId The user ID of the user to unsubscribe from the thread.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async unsubscribeFromThread(params, options) {
        const { roomId, threadId } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/unsubscribe`, {
            userId: params.data.userId
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Updates the metadata of the specified thread in a room.
   * @param params.roomId The room ID to update the thread in.
   * @param params.threadId The thread ID to update.
   * @param params.data.metadata The metadata for the thread. Value must be a string, boolean or number
   * @param params.data.userId The user ID of the user who updated the thread.
   * @param params.data.updatedAt (optional) The date the thread is set to be updated.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The updated thread metadata.
   */ async editThreadMetadata(params, options) {
        const { roomId, threadId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/metadata`, {
            ...data,
            updatedAt: data.updatedAt?.toISOString()
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Updates the metadata of the specified comment in a room.
   * @param params.roomId The room ID to update the comment in.
   * @param params.threadId The thread ID to update the comment in.
   * @param params.commentId The comment ID to update.
   * @param params.data.metadata The metadata for the comment. Value must be a string, boolean or number. Use null to delete a key.
   * @param params.data.userId The user ID of the user who updated the comment.
   * @param params.data.updatedAt (optional) The date the comment metadata is set to be updated.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The updated comment metadata.
   */ async editCommentMetadata(params, options) {
        const { roomId, threadId, commentId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/comments/${commentId}/metadata`, {
            ...data,
            updatedAt: data.updatedAt?.toISOString()
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Adds a new comment reaction to a comment.
   * @param params.roomId The room ID to add the comment reaction in.
   * @param params.threadId The thread ID to add the comment reaction in.
   * @param params.commentId The comment ID to add the reaction in.
   * @param params.data.emoji The (emoji) reaction to add.
   * @param params.data.userId The user ID of the user associated with the reaction.
   * @param params.data.createdAt (optional) The date the reaction is set to be created.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The created comment reaction.
   */ async addCommentReaction(params, options) {
        const { roomId, threadId, commentId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/comments/${commentId}/add-reaction`, {
            ...data,
            createdAt: data.createdAt?.toISOString()
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const reaction = await res.json();
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToCommentUserReaction"])(reaction);
    }
    /**
   * Removes a reaction from a comment.
   * @param params.roomId The room ID to remove the comment reaction from.
   * @param params.threadId The thread ID to remove the comment reaction from.
   * @param params.commentId The comment ID to remove the reaction from.
   * @param params.data.emoji The (emoji) reaction to remove.
   * @param params.data.userId The user ID of the user associated with the reaction.
   * @param params.data.removedAt (optional) The date the reaction is set to be removed.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async removeCommentReaction(params, options) {
        const { roomId, threadId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/threads/${threadId}/comments/${params.commentId}/remove-reaction`, {
            ...data,
            removedAt: data.removedAt?.toISOString()
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Returns the inbox notifications for a user.
   * @param params.userId The user ID to get the inbox notifications from.
   * @param params.inboxNotificationId The ID of the inbox notification to get.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getInboxNotification(params, options) {
        const { userId, inboxNotificationId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/inbox-notifications/${inboxNotificationId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToInboxNotificationData"])(await res.json());
    }
    /**
   * Returns the inbox notifications for a user.
   * @param params.userId The user ID to get the inbox notifications from.
   * @param params.query The query to filter inbox notifications by. It is based on our query language and can filter by unread.
   * @param params.organizationId (optional) The organization ID to get the inbox notifications for.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getInboxNotifications(params, options) {
        const { userId, tenantId, organizationId, limit, startingAfter } = params;
        let query;
        if (typeof params.query === "string") {
            query = params.query;
        } else if (typeof params.query === "object") {
            query = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["objectToQuery"])(params.query);
        }
        const queryParams = {
            query,
            limit,
            startingAfter
        };
        if (organizationId !== void 0) {
            queryParams.organizationId = organizationId;
        } else if (tenantId !== void 0) {
            queryParams.organizationId = tenantId;
        }
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/inbox-notifications`, queryParams, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        return {
            ...page,
            data: page.data.map(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToInboxNotificationData"])
        };
    }
    /**
   * Iterates over all inbox notifications for a user.
   *
   * The difference with .getInboxNotifications() is that pagination will
   * happen automatically under the hood, using the given `pageSize`.
   *
   * @param criteria.userId The user ID to get the inbox notifications from.
   * @param criteria.query The query to filter inbox notifications by. It is based on our query language and can filter by unread.
   * @param criteria.organizationId (optional) The organization ID to get the inbox notifications for.
   * @param options.pageSize (optional) The page size to use for each request.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async *iterInboxNotifications(criteria, options) {
        const { signal } = options ?? {};
        const pageSize = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkBounds"])("pageSize", options?.pageSize ?? 50, 10);
        let cursor = void 0;
        while(true){
            const { nextCursor, data } = await this.getInboxNotifications({
                ...criteria,
                startingAfter: cursor,
                limit: pageSize
            }, {
                signal
            });
            for (const item of data){
                yield item;
            }
            if (!nextCursor) {
                break;
            }
            cursor = nextCursor;
        }
    }
    /**
   * Returns all room subscription settings for a user.
   * @param params.userId The user ID to get the room subscription settings from.
   * @param params.organizationId (optional) The organization ID to get the room subscription settings for.
   * @param params.startingAfter (optional) The cursor to start the pagination from.
   * @param params.limit (optional) The number of items to return.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getUserRoomSubscriptionSettings(params, options) {
        const { userId, tenantId, organizationId, startingAfter, limit } = params;
        const queryParams = {
            startingAfter,
            limit
        };
        if (organizationId !== void 0) {
            queryParams.organizationId = organizationId;
        } else if (tenantId !== void 0) {
            queryParams.organizationId = tenantId;
        }
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/room-subscription-settings`, queryParams, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Gets the user's room subscription settings.
   * @param params.userId The user ID to get the room subscription settings from.
   * @param params.roomId The room ID to get the room subscription settings from.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getRoomSubscriptionSettings(params, options) {
        const { userId, roomId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/users/${userId}/subscription-settings`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Updates the user's room subscription settings.
   * @param params.userId The user ID to update the room subscription settings for.
   * @param params.roomId The room ID to update the room subscription settings for.
   * @param params.data The new room subscription settings for the user.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async updateRoomSubscriptionSettings(params, options) {
        const { userId, roomId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/users/${userId}/subscription-settings`, data, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Delete the user's room subscription settings.
   * @param params.userId The user ID to delete the room subscription settings from.
   * @param params.roomId The room ID to delete the room subscription settings from.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteRoomSubscriptionSettings(params, options) {
        const { userId, roomId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/users/${userId}/subscription-settings`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Update a room ID.
   * @param params.roomId The current ID of the room.
   * @param params.newRoomId The new room ID.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async updateRoomId(params, options) {
        const { currentRoomId, newRoomId } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${currentRoomId}/update-room-id`, {
            newRoomId
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateRoomData(data);
    }
    /**
   * Triggers an inbox notification for a user.
   * @param params.userId The user ID to trigger the inbox notification for.
   * @param params.kind The kind of inbox notification to trigger.
   * @param params.subjectId The subject ID of the triggered inbox notification.
   * @param params.activityData The activity data of the triggered inbox notification.
   * @param params.roomId (optional) The room ID to trigger the inbox notification for.
   * @param params.organizationId (optional) The organization ID to trigger the inbox notification for.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async triggerInboxNotification(params, options) {
        const { tenantId, organizationId, ...restParams } = params;
        const body = {
            ...restParams
        };
        if (organizationId !== void 0) {
            body.organizationId = organizationId;
        } else if (tenantId !== void 0) {
            body.organizationId = tenantId;
        }
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/inbox-notifications/trigger`, body, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Deletes an inbox notification for a user.
   * @param params.userId The user ID for which to delete the inbox notification.
   * @param params.inboxNotificationId The ID of the inbox notification to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteInboxNotification(params, options) {
        const { userId, inboxNotificationId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/inbox-notifications/${inboxNotificationId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Deletes all inbox notifications for a user.
   * @param params.userId The user ID for which to delete all the inbox notifications.
   * @param params.organizationId (optional) The organization ID to delete the inbox notifications for.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteAllInboxNotifications(params, options) {
        const { userId, tenantId, organizationId } = params;
        const queryParams = {};
        if (organizationId !== void 0) {
            queryParams.organizationId = organizationId;
        } else if (tenantId !== void 0) {
            queryParams.organizationId = tenantId;
        }
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/inbox-notifications`, queryParams, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Get notification settings for a user for a project.
   * @param params.userId The user ID to get the notifications settings for.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getNotificationSettings(params, options) {
        const { userId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/notification-settings`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const plainSettings = await res.json();
        const settings = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createNotificationSettings"])(plainSettings);
        return settings;
    }
    /**
   * Update the user's notification settings.
   * @param params.userId The user ID to update the notification settings for.
   * @param params.data The new notification settings for the user.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async updateNotificationSettings(params, options) {
        const { userId, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/notification-settings`, data, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const plainSettings = await res.json();
        const settings = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createNotificationSettings"])(plainSettings);
        return settings;
    }
    /**
   * Delete the user's notification settings
   * @param params.userId The user ID to update the notification settings for.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteNotificationSettings(params, options) {
        const { userId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/notification-settings`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Create a group
   * @param params.groupId The ID of the group to create.
   * @param params.memberIds The IDs of the members to add to the group.
   * @param params.organizationId (optional) The organization ID to create the group for.
   * @param params.scopes (optional) The scopes to grant to the group. The default is `{ mention: true }`.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async createGroup(params, options) {
        const { tenantId, organizationId, ...restParams } = params;
        const body = {
            ...restParams,
            // The REST API uses `id` since a group is a resource,
            // but we use `groupId` here for consistency with the other methods.
            id: params.groupId
        };
        if (organizationId !== void 0) {
            body.organizationId = organizationId;
        } else if (tenantId !== void 0) {
            body.organizationId = tenantId;
        }
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/groups`, body, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const group = await res.json();
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToGroupData"])(group);
    }
    /**
   * Get a group
   * @param params.groupId The ID of the group to get.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getGroup(params, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/groups/${params.groupId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const group = await res.json();
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToGroupData"])(group);
    }
    /**
   * Add members to a group
   * @param params.groupId The ID of the group to add members to.
   * @param params.memberIds The IDs of the members to add to the group.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async addGroupMembers(params, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/groups/${params.groupId}/add-members`, {
            memberIds: params.memberIds
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const group = await res.json();
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToGroupData"])(group);
    }
    /**
   * Remove members from a group
   * @param params.groupId The ID of the group to remove members from.
   * @param params.memberIds The IDs of the members to remove from the group.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async removeGroupMembers(params, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/groups/${params.groupId}/remove-members`, {
            memberIds: params.memberIds
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const group = await res.json();
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToGroupData"])(group);
    }
    /**
   * Delete a group
   * @param params.groupId The ID of the group to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteGroup(params, options) {
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/groups/${params.groupId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Get all groups
   * @param params.limit (optional) The number of groups to return.
   * @param params.startingAfter (optional) The cursor to start the pagination from.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getGroups(params, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/groups`, {
            startingAfter: params?.startingAfter,
            limit: params?.limit
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        return {
            ...page,
            data: page.data.map(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToGroupData"])
        };
    }
    /**
   * Returns all groups a user is a member of.
   * @param params.userId The user ID to get the groups for.
   * @param params.startingAfter (optional) The cursor to start the pagination from.
   * @param params.limit (optional) The number of items to return.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getUserGroups(params, options) {
        const { userId, startingAfter, limit } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/users/${userId}/groups`, {
            startingAfter,
            limit
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        return {
            ...page,
            data: page.data.map(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToGroupData"])
        };
    }
    /**
   * Retrieves the current Storage contents for the given room ID and calls the
   * provided callback function, in which you can mutate the Storage contents
   * at will.
   *
   * If you need to run the same mutation across multiple rooms, prefer using
   * `.massMutateStorage()` instead of looping over room IDs yourself.
   */ async mutateStorage(roomId, callback, options) {
        return this.#_mutateOneRoom(roomId, void 0, callback, options);
    }
    /**
   * Retrieves the Storage contents for each room that matches the given
   * criteria and calls the provided callback function, in which you can mutate
   * the Storage contents at will.
   *
   * You can use the `criteria` parameter to select which rooms to process by
   * their metadata. If you pass `{}` (empty object), all rooms will be
   * selected and processed.
   *
   * This method will execute mutations in parallel, using the specified
   * `concurrency` value. If you which to run the mutations serially, set
   * `concurrency` to 1.
   */ async massMutateStorage(criteria, callback, massOptions) {
        const concurrency = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkBounds"])("concurrency", massOptions?.concurrency ?? 8, 1, 20);
        const pageSize = Math.max(20, concurrency * 4);
        const { signal } = massOptions ?? {};
        const rooms = this.iterRooms(criteria, {
            pageSize,
            signal
        });
        const options = {
            signal
        };
        await runConcurrently(rooms, (roomData)=>this.#_mutateOneRoom(roomData.id, roomData, callback, options), concurrency);
    }
    async #_mutateOneRoom(roomId, room, callback, options) {
        const debounceInterval = 200;
        const { signal, abort } = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["makeAbortController"])(options?.signal);
        let opsBuffer = [];
        let outstandingFlush$ = void 0;
        let lastFlush = performance.now();
        const flushIfNeeded = (force)=>{
            if (opsBuffer.length === 0) return;
            if (outstandingFlush$) {
                return;
            }
            const now = performance.now();
            if (!(force || now - lastFlush > debounceInterval)) {
                return;
            }
            lastFlush = now;
            const ops = opsBuffer;
            opsBuffer = [];
            outstandingFlush$ = this.#sendMessage(roomId, [
                {
                    type: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ClientMsgCode"].UPDATE_STORAGE,
                    ops
                }
            ], {
                signal
            }).catch((err)=>{
                abort(err);
            }).finally(()=>{
                outstandingFlush$ = void 0;
            });
        };
        try {
            const resp = await this.#requestStorageMutation(roomId, {
                signal
            });
            const { actor, nodes } = resp;
            const pool = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createManagedPool"])({
                getCurrentConnectionId: ()=>actor,
                onDispatch: (ops, _reverse, _storageUpdates)=>{
                    if (ops.length === 0) return;
                    for (const op of ops){
                        opsBuffer.push(op);
                    }
                    flushIfNeeded(/* force */ false);
                }
            });
            const root = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["LiveObject"]._fromItems(nodes, pool);
            const callback$ = callback({
                room,
                root
            });
            flushIfNeeded(/* force */ true);
            await callback$;
        } catch (e) {
            abort();
            throw e;
        } finally{
            await outstandingFlush$;
            flushIfNeeded(/* force */ true);
            await outstandingFlush$;
        }
    }
    async #sendMessage(roomId, messages, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/send-message`, {
            messages
        }, {
            signal: options?.signal
        });
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Returns a paginated list of AI copilots. The copilots are returned sorted by creation date, from newest to oldest.
   * @param params.limit (optional) A limit on the number of copilots to return. The limit can range between 1 and 100, and defaults to 20.
   * @param params.startingAfter (optional) A cursor used for pagination. You get the value from the response of the previous page.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A paginated list of AI copilots.
   */ async getAiCopilots(params = {}, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots`, {
            limit: params.limit,
            startingAfter: params.startingAfter
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        return {
            ...page,
            data: page.data.map(inflateAiCopilot)
        };
    }
    /**
   * Creates an AI copilot.
   * @param params The parameters to create the copilot with.
   * @returns The created copilot.
   */ async createAiCopilot(params, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots`, params, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateAiCopilot(data);
    }
    /**
   * Returns an AI copilot with the given id.
   * @param copilotId The id of the copilot to return.
   * @returns The copilot with the given id.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async getAiCopilot(copilotId, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${copilotId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateAiCopilot(data);
    }
    /**
   * Updates an AI copilot with the given id.
   * @param copilotId The id of the copilot to update.
   * @param params The parameters to update the copilot with.
   * @returns The updated copilot.
   */ async updateAiCopilot(copilotId, params, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${copilotId}`, params, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateAiCopilot(data);
    }
    /**
   * Deletes an AI copilot with the given id. A deleted copilot is no longer accessible from the API or the dashboard and it cannot be restored.
   * @param copilotId The id of the copilot to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteAiCopilot(copilotId, options) {
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${copilotId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Creates a web knowledge source.
   * @param params.url The URL of the web knowledge source.
   * @param params.type The type of the web knowledge source: "individual_link", "crawl" or "sitemap".
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The id of the created web knowledge source.
   */ async createWebKnowledgeSource(params, options) {
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge/web`, params, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return data;
    }
    /**
   * Creates a file knowledge source.
   * @param params.copilotId The id of the copilot.
   * @param params.name The name of the file knowledge source.
   * @param params.file The file to create the knowledge source from.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The id of the created file knowledge source.
   */ async createFileKnowledgeSource(params, options) {
        const fetch = await fetchPolyfill();
        const res = await fetch((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["urljoin"])(this.#baseUrl, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge/file/${params.file.name}`), {
            method: "PUT",
            body: params.file,
            headers: {
                Authorization: `Bearer ${this.#secret}`,
                "Content-Type": params.file.type,
                "Content-Length": String(params.file.size)
            },
            signal: options?.signal
        });
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return data;
    }
    /**
   * Deletes a file knowledge source.
   * @param params.copilotId The id of the copilot.
   * @param params.knowledgeSourceId The id of the knowledge source to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteFileKnowledgeSource(params, options) {
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge/file/${params.knowledgeSourceId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Deletes a web knowledge source.
   * @param params.copilotId The id of the copilot.
   * @param params.knowledgeSourceId The id of the knowledge source to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteWebKnowledgeSource(params, options) {
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge/web/${params.knowledgeSourceId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Returns a paginated list of knowledge sources.
   * @param params.copilotId The id of the copilot.
   * @param params.limit (optional) A limit on the number of knowledge sources to return. The limit can range between 1 and 100, and defaults to 20.
   * @param params.startingAfter (optional) A cursor used for pagination. You get the value from the response of the previous page.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A paginated list of knowledge sources.
   */ async getKnowledgeSources(params, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge`, {
            limit: params.limit,
            startingAfter: params.startingAfter
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        return {
            ...page,
            data: page.data.map(inflateKnowledgeSource)
        };
    }
    /**
   * Returns a knowledge source with the given id.
   * @param params.copilotId The id of the copilot.
   * @param params.knowledgeSourceId The id of the knowledge source to return.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The knowledge source.
   */ async getKnowledgeSource(params, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge/${params.knowledgeSourceId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return inflateKnowledgeSource(data);
    }
    /**
   * Returns the content of a file knowledge source.
   * @param params.copilotId The id of the copilot.
   * @param params.knowledgeSourceId The id of the knowledge source.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The content of the file knowledge source.
   */ async getFileKnowledgeSourceMarkdown(params, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge/file/${params.knowledgeSourceId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const data = await res.json();
        return data.content;
    }
    /**
   * Returns a paginated list of web knowledge source links.
   * @param params.copilotId The id of the copilot.
   * @param params.knowledgeSourceId The id of the knowledge source.
   * @param params.limit (optional) A limit on the number of links to return. The limit can range between 1 and 100, and defaults to 20.
   * @param params.startingAfter (optional) A cursor used for pagination. You get the value from the response of the previous page.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A paginated list of web knowledge source links.
   */ async getWebKnowledgeSourceLinks(params, options) {
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/ai/copilots/${params.copilotId}/knowledge/web/${params.knowledgeSourceId}/links`, {
            limit: params.limit,
            startingAfter: params.startingAfter
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        const page = await res.json();
        return {
            ...page,
            data: page.data.map(inflateWebKnowledgeSourceLink)
        };
    }
    /* -------------------------------------------------------------------------------------------------
   * Feeds
   * -----------------------------------------------------------------------------------------------*/ /**
   * Returns a list of feeds in a room.
   * @param params.roomId The room ID to get the feeds from.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A list of feeds.
   */ async getFeeds(params, options) {
        const { roomId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Creates a new feed in a room.
   * @param params.roomId The room ID to create the feed in.
   * @param params.feedId The feed ID.
   * @param params.metadata (optional) The metadata for the feed.
   * @param params.createdAt (optional) Creation time in ms. Sent to the API as `timestamp`. If not provided, the server uses the current time.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The created feed.
   */ async createFeed(params, options) {
        const { roomId, feedId, metadata, createdAt } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds`, {
            feedId,
            ...metadata !== void 0 ? {
                metadata
            } : {},
            ...createdAt !== void 0 ? {
                timestamp: createdAt
            } : {}
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Returns a feed with the given id.
   * @param params.roomId The room ID to get the feed from.
   * @param params.feedId The feed ID.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The feed.
   */ async getFeed(params, options) {
        const { roomId, feedId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds/${feedId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Updates the metadata of a feed.
   * @param params.roomId The room ID to update the feed in.
   * @param params.feedId The feed ID to update.
   * @param params.metadata The metadata for the feed.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The updated feed.
   */ async updateFeed(params, options) {
        const { roomId, feedId, metadata } = params;
        const res = await this.#patch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds/${feedId}`, {
            metadata
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Deletes a feed.
   * @param params.roomId The room ID to delete the feed from.
   * @param params.feedId The feed ID to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteFeed(params, options) {
        const { roomId, feedId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds/${feedId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
    /**
   * Returns a list of messages in a feed.
   * @param params.roomId The room ID to get the feed messages from.
   * @param params.feedId The feed ID to get the messages from.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns A list of feed messages.
   */ async getFeedMessages(params, options) {
        const { roomId, feedId } = params;
        const res = await this.#get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds/${feedId}/messages`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Creates a new message in a feed.
   * @param params.roomId The room ID to create the feed message in.
   * @param params.feedId The feed ID to create the message in.
   * @param params.id (optional) The message ID. If not provided, one will be generated.
   * @param params.createdAt (optional) Creation time in ms. Sent to the API as `timestamp`. If not provided, the server uses the current time.
   * @param params.data The message data.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The created feed message.
   */ async createFeedMessage(params, options) {
        const { roomId, feedId, id, createdAt, data } = params;
        const res = await this.#post(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds/${feedId}/messages`, {
            data,
            ...id !== void 0 ? {
                id
            } : {},
            ...createdAt !== void 0 ? {
                timestamp: createdAt
            } : {}
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Updates a feed message.
   * @param params.roomId The room ID to update the feed message in.
   * @param params.feedId The feed ID to update the message in.
   * @param params.messageId The message ID to update.
   * @param params.data The message data.
   * @param params.updatedAt (optional) Update time in ms. Sent to the API as `timestamp`. If omitted, the server uses the current time.
   * @param options.signal (optional) An abort signal to cancel the request.
   * @returns The updated feed message.
   */ async updateFeedMessage(params, options) {
        const { roomId, feedId, messageId, data, updatedAt } = params;
        const res = await this.#patch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds/${feedId}/messages/${messageId}`, {
            data,
            ...updatedAt !== void 0 ? {
                timestamp: updatedAt
            } : {}
        }, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
        return await res.json();
    }
    /**
   * Deletes a feed message.
   * @param params.roomId The room ID to delete the feed message from.
   * @param params.feedId The feed ID to delete the message from.
   * @param params.messageId The message ID to delete.
   * @param options.signal (optional) An abort signal to cancel the request.
   */ async deleteFeedMessage(params, options) {
        const { roomId, feedId, messageId } = params;
        const res = await this.#delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["url"]`/v2/rooms/${roomId}/feeds/${feedId}/messages/${messageId}`, void 0, options);
        if (!res.ok) {
            throw await LiveblocksError.from(res);
        }
    }
};
var LiveblocksError = class _LiveblocksError extends Error {
    status;
    details;
    constructor(message, status, details){
        super(message);
        this.name = "LiveblocksError";
        this.status = status;
        this.details = details;
    }
    toString() {
        let msg = `${this.name}: ${this.message} (status ${this.status})`;
        if (this.details) {
            msg += `
${this.details}`;
        }
        return msg;
    }
    static async from(res) {
        const origErrLocation = new Error();
        Error.captureStackTrace(origErrLocation, _LiveblocksError.from);
        const FALLBACK = "An error happened without an error message";
        let text;
        try {
            text = await res.text();
        } catch  {
            text = FALLBACK;
        }
        const obj = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["tryParseJson"])(text) ?? {
            message: text
        };
        const message = obj.message || FALLBACK;
        const details = [
            obj.suggestion ? `Suggestion: ${String(obj.suggestion)}` : void 0,
            obj.docs ? `See also: ${String(obj.docs)}` : void 0
        ].filter(Boolean).join("\n") || void 0;
        const err = new _LiveblocksError(message, res.status, details);
        err.stack = origErrLocation.stack;
        return err;
    }
};
;
;
var MENTION_REGEX = /(^|[^A-Za-z0-9_.-])@([A-Za-z0-9_][A-Za-z0-9_.@-]*)/g;
var MENTION_TRAILING_PUNCTUATION_REGEX = /[.-]/;
var WHITESPACE_REGEX = /\s/;
var MarkedCustomTokenizer = class extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$marked$2f$lib$2f$marked$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Tokenizer"] {
    url(src) {
        const token = super.url(src);
        if (token?.href.startsWith("mailto:")) {
            return void 0;
        }
        return token;
    }
};
function assertTokens(_) {}
function tokenizeMarkdown(markdown) {
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$marked$2f$lib$2f$marked$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Lexer"]({
        gfm: true,
        tokenizer: new MarkedCustomTokenizer()
    }).lex(markdown);
}
function toCommentBodyText(text, formatting) {
    return {
        text,
        ...formatting
    };
}
function appendPlainText(inlines, text, formatting) {
    if (text.length === 0) {
        return;
    }
    inlines.push(toCommentBodyText(text, formatting));
}
function listIndent(listDepth) {
    return "  ".repeat(listDepth);
}
function blockquotePrefix(blockquoteDepth) {
    return blockquoteDepth > 0 ? "> ".repeat(blockquoteDepth) : "";
}
function listMarker(ordered, start, index) {
    if (!ordered) {
        return "- ";
    }
    const firstItemNumber = start === "" ? 1 : start;
    return `${firstItemNumber + index}. `;
}
function taskListPrefix(item) {
    if (!item.task) {
        return "";
    }
    return item.checked ? "[x] " : "[ ] ";
}
function prependTextToParagraph(paragraph, textPrefix) {
    if (textPrefix.length === 0) {
        return paragraph;
    }
    return {
        ...paragraph,
        children: [
            {
                text: textPrefix
            },
            ...paragraph.children
        ]
    };
}
function appendTextWithMentions(inlines, text, formatting) {
    let cursor = 0;
    for (const match of text.matchAll(MENTION_REGEX)){
        const prefix = match[1] ?? "";
        const rawId = match[2];
        if (match.index === void 0 || rawId === void 0) {
            continue;
        }
        const atSignIndex = match.index + prefix.length;
        const idStart = atSignIndex + 1;
        let idEnd = idStart + rawId.length;
        while(idEnd > idStart && MENTION_TRAILING_PUNCTUATION_REGEX.test(text.charAt(idEnd - 1)) && (idEnd === text.length || WHITESPACE_REGEX.test(text.charAt(idEnd)))){
            idEnd -= 1;
        }
        if (atSignIndex > cursor) {
            inlines.push(toCommentBodyText(text.slice(cursor, atSignIndex), formatting));
        }
        inlines.push({
            type: "mention",
            kind: "user",
            id: text.slice(idStart, idEnd)
        });
        cursor = idEnd;
    }
    if (cursor < text.length) {
        inlines.push(toCommentBodyText(text.slice(cursor), formatting));
    }
}
function appendFormattedInlinesFromTokens(inlines, tokens, formatting) {
    inlines.push(...tokensToCommentBodyInlines(tokens, formatting));
}
function tableAlignmentMarker(alignment) {
    switch(alignment){
        case "left":
            return ":---";
        case "center":
            return ":---:";
        case "right":
            return "---:";
        default:
            return "---";
    }
}
function tableToMarkdownRows(table) {
    const rows = [
        table.header,
        ...table.rows
    ];
    const markdownRows = rows.map((row)=>{
        const cells = row.map((cell)=>tokensToPlainText(cell.tokens));
        return `| ${cells.join(" | ")} |`;
    });
    if (markdownRows.length === 0) {
        return [];
    }
    const separatorRow = `| ${table.align.map(tableAlignmentMarker).join(" | ")} |`;
    return [
        markdownRows[0],
        separatorRow,
        ...markdownRows.slice(1)
    ].filter((row)=>row !== void 0);
}
function tokensToPlainText(tokens, listDepth = 0) {
    assertTokens(tokens);
    return tokens.map((t)=>tokenToPlainText(t, listDepth)).join("");
}
function tokenToPlainText(token, listDepth = 0) {
    switch(token.type){
        case "escape":
        case "html":
        case "codespan":
        case "text":
            {
                return token.text;
            }
        case "br":
            {
                return "\n";
            }
        case "strong":
        case "em":
        case "del":
        case "link":
            {
                return tokensToPlainText(token.tokens, 0);
            }
        case "image":
            {
                return token.text || token.href;
            }
        case "paragraph":
        case "heading":
            {
                return tokensToPlainText(token.tokens, listDepth);
            }
        case "blockquote":
            {
                return tokensToPlainText(token.tokens, listDepth);
            }
        case "list":
            {
                return token.items.map((item, index)=>listItemToText(item, token.ordered, token.start, index, listDepth)).join("\n");
            }
        case "list_item":
            {
                return tokensToPlainText(token.tokens, listDepth);
            }
        case "code":
            {
                return token.text;
            }
        case "table":
            {
                return tableToMarkdownRows(token).join("\n");
            }
        case "space":
        case "hr":
            {
                return "";
            }
        default:
            {
                return "";
            }
    }
}
function listItemToText(item, ordered, start, index, listDepth) {
    const indent = listIndent(listDepth);
    const marker = listMarker(ordered, start, index);
    const prefix = indent + marker + taskListPrefix(item);
    return `${prefix}${tokensToPlainText(item.tokens, listDepth + 1)}`;
}
function tokensToCommentBodyInlines(tokens, formatting = {}) {
    assertTokens(tokens);
    const inlines = [];
    for (const token of tokens){
        switch(token.type){
            case "escape":
            case "html":
            case "text":
                {
                    if (token.type === "text" && token.tokens?.length) {
                        appendFormattedInlinesFromTokens(inlines, token.tokens, formatting);
                    } else {
                        appendTextWithMentions(inlines, token.text, formatting);
                    }
                    break;
                }
            case "br":
                {
                    inlines.push(toCommentBodyText("\n", formatting));
                    break;
                }
            case "strong":
                {
                    appendFormattedInlinesFromTokens(inlines, token.tokens, {
                        ...formatting,
                        bold: true
                    });
                    break;
                }
            case "em":
                {
                    appendFormattedInlinesFromTokens(inlines, token.tokens, {
                        ...formatting,
                        italic: true
                    });
                    break;
                }
            case "del":
                {
                    appendFormattedInlinesFromTokens(inlines, token.tokens, {
                        ...formatting,
                        strikethrough: true
                    });
                    break;
                }
            case "codespan":
                {
                    inlines.push(toCommentBodyText(token.text, {
                        ...formatting,
                        code: true
                    }));
                    break;
                }
            case "link":
                {
                    const href = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["sanitizeUrl"])(token.href);
                    const text = tokensToPlainText(token.tokens, 0);
                    if (href === null) {
                        appendPlainText(inlines, text, formatting);
                        break;
                    }
                    inlines.push({
                        type: "link",
                        url: href,
                        text
                    });
                    break;
                }
            case "image":
                {
                    appendPlainText(inlines, token.raw, formatting);
                    break;
                }
            default:
                {
                    const text = tokenToPlainText(token, 0);
                    if (text) {
                        appendTextWithMentions(inlines, text, formatting);
                    }
                    break;
                }
        }
    }
    return inlines;
}
function tokenToCommentBodyParagraphs(token, listDepth = 0, blockquoteDepth = 0) {
    switch(token.type){
        case "space":
        case "hr":
            {
                return [];
            }
        case "paragraph":
        case "heading":
            {
                const children = tokensToCommentBodyInlines(token.tokens);
                if (children.length === 0) {
                    return [];
                }
                const headingPrefix = token.type === "heading" ? `${"#".repeat(token.depth)} ` : "";
                return [
                    prependTextToParagraph({
                        type: "paragraph",
                        children
                    }, blockquotePrefix(blockquoteDepth) + headingPrefix)
                ];
            }
        case "blockquote":
            {
                return tokensToCommentBodyParagraphs(token.tokens, listDepth, blockquoteDepth + 1);
            }
        case "list":
            {
                const indent = listIndent(listDepth);
                const quotePrefix = blockquotePrefix(blockquoteDepth);
                return token.items.flatMap((item, index)=>{
                    const marker = listMarker(token.ordered, token.start, index);
                    const listPrefix = indent + marker + taskListPrefix(item);
                    const paragraphs = tokensToCommentBodyParagraphs(item.tokens, listDepth + 1, 0);
                    const [firstParagraph, ...remainingParagraphs] = paragraphs;
                    if (!firstParagraph) {
                        return [
                            {
                                type: "paragraph",
                                children: [
                                    {
                                        text: quotePrefix + listPrefix
                                    }
                                ]
                            }
                        ];
                    }
                    const firstParagraphWithPrefixes = prependTextToParagraph(firstParagraph, quotePrefix + listPrefix);
                    const remainingParagraphsWithQuotePrefix = remainingParagraphs.map((paragraph)=>prependTextToParagraph(paragraph, quotePrefix));
                    return [
                        firstParagraphWithPrefixes,
                        ...remainingParagraphsWithQuotePrefix
                    ];
                });
            }
        case "list_item":
            {
                return tokensToCommentBodyParagraphs(token.tokens, listDepth, blockquoteDepth);
            }
        case "code":
            {
                return [
                    prependTextToParagraph({
                        type: "paragraph",
                        children: [
                            {
                                text: token.text
                            }
                        ]
                    }, blockquotePrefix(blockquoteDepth))
                ];
            }
        case "table":
            {
                const quotePrefix = blockquotePrefix(blockquoteDepth);
                return tableToMarkdownRows(token).map((row)=>({
                        type: "paragraph",
                        children: [
                            {
                                text: quotePrefix + row
                            }
                        ]
                    }));
            }
        default:
            {
                const inlines = tokensToCommentBodyInlines([
                    token
                ]);
                return inlines.length > 0 ? [
                    prependTextToParagraph({
                        type: "paragraph",
                        children: inlines
                    }, blockquotePrefix(blockquoteDepth))
                ] : [];
            }
    }
}
function tokensToCommentBodyParagraphs(tokens, listDepth = 0, blockquoteDepth = 0) {
    assertTokens(tokens);
    return tokens.flatMap((token)=>tokenToCommentBodyParagraphs(token, listDepth, blockquoteDepth));
}
function markdownToCommentBody(markdown) {
    return {
        version: 1,
        content: tokensToCommentBodyParagraphs(tokenizeMarkdown(markdown))
    };
}
;
;
var WebhookHandler = class _WebhookHandler {
    #secretBuffer;
    static #secretPrefix = "whsec_";
    constructor(secret){
        if (!secret) throw new Error("Secret is required");
        if (typeof secret !== "string") throw new Error("Secret must be a string");
        if (secret.startsWith(_WebhookHandler.#secretPrefix) === false) throw new Error("Invalid secret, must start with whsec_");
        const secretKey = secret.slice(_WebhookHandler.#secretPrefix.length);
        this.#secretBuffer = Buffer.from(secretKey, "base64");
    }
    /**
   * Verifies a webhook request and returns the event
   */ verifyRequest(request) {
        const { headers, rawBody } = request;
        const { webhookId, timestamp, rawSignatures } = this.#verifyHeaders(headers);
        if (typeof rawBody !== "string") {
            throw new Error(`Invalid rawBody field, must be a string, got "${typeof rawBody}" instead. It is likely that you need to JSON.stringify the body before passing it.`);
        }
        this.#verifyTimestamp(timestamp);
        const signature = this.#sign(`${webhookId}.${timestamp}.${rawBody}`);
        const expectedSignatures = rawSignatures.split(" ").map((rawSignature)=>{
            const [, parsedSignature] = rawSignature.split(",");
            return parsedSignature;
        }).filter(isNotUndefined);
        if (expectedSignatures.includes(signature) === false) throw new Error(`Invalid signature, expected one of ${expectedSignatures.join(", ")}, got ${signature}`);
        const event = JSON.parse(rawBody);
        this.#verifyWebhookEventType(event);
        return event;
    }
    /**
   * Verifies the headers and returns the webhookId, timestamp and rawSignatures
   */ #verifyHeaders(headers) {
        const usingNativeHeaders = typeof Headers !== "undefined" && headers instanceof Headers;
        const normalizedHeaders = usingNativeHeaders ? Object.fromEntries(headers) : headers;
        const sanitizedHeaders = {};
        Object.keys(normalizedHeaders).forEach((key)=>{
            sanitizedHeaders[key.toLowerCase()] = normalizedHeaders[key];
        });
        const webhookId = sanitizedHeaders["webhook-id"];
        if (typeof webhookId !== "string") throw new Error("Invalid webhook-id header");
        const timestamp = sanitizedHeaders["webhook-timestamp"];
        if (typeof timestamp !== "string") throw new Error("Invalid webhook-timestamp header");
        const rawSignatures = sanitizedHeaders["webhook-signature"];
        if (typeof rawSignatures !== "string") throw new Error("Invalid webhook-signature header");
        return {
            webhookId,
            timestamp,
            rawSignatures
        };
    }
    /**
   * Signs the content with the secret
   * @param content
   * @returns `string`
   */ #sign(content) {
        const encoder = new TextEncoder();
        const toSign = encoder.encode(content);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stablelib$2f$base64$2f$lib$2f$base64$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["encode"](__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$fast$2d$sha256$2f$sha256$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["hmac"](this.#secretBuffer, toSign));
    }
    /**
   * Verifies that the timestamp is not too old or in the future
   */ #verifyTimestamp(timestampHeader) {
        const now = Math.floor(Date.now() / 1e3);
        const timestamp = parseInt(timestampHeader, 10);
        if (isNaN(timestamp)) {
            throw new Error("Invalid timestamp");
        }
        if (timestamp < now - WEBHOOK_TOLERANCE_IN_SECONDS) {
            throw new Error("Timestamp too old");
        }
        if (timestamp > now + WEBHOOK_TOLERANCE_IN_SECONDS) {
            throw new Error("Timestamp in the future");
        }
    }
    /**
   * Ensures that the event is a known event type
   * or throws and prompts the user to upgrade to a higher version of @liveblocks/node
   */ #verifyWebhookEventType(event) {
        if (event && event.type && [
            "storageUpdated",
            "userEntered",
            "userLeft",
            "roomCreated",
            "roomDeleted",
            "commentCreated",
            "commentEdited",
            "commentDeleted",
            "commentReactionAdded",
            "commentReactionRemoved",
            "commentMetadataUpdated",
            "threadMetadataUpdated",
            "threadCreated",
            "threadDeleted",
            "ydocUpdated",
            "notification",
            "threadMarkedAsResolved",
            "threadMarkedAsUnresolved"
        ].includes(event.type)) {
            if (event.type === "notification") {
                const notification = event;
                if (notification.data.kind === "thread" || notification.data.kind === "textMention" || isCustomKind(notification.data.kind)) {
                    return;
                } else {
                    throw new Error(`Unknown notification kind: ${JSON.stringify(notification.data.kind)}`);
                }
            }
            return;
        }
        throw new Error("Unknown event type, please upgrade to a higher version of @liveblocks/node");
    }
};
var WEBHOOK_TOLERANCE_IN_SECONDS = 5 * 60;
var isNotUndefined = (value)=>value !== void 0;
var isCustomKind = (value)=>{
    return isString(value) && value.startsWith("$");
};
function isThreadNotificationEvent(event) {
    return event.type === "notification" && event.data.kind === "thread";
}
function isTextMentionNotificationEvent(event) {
    return event.type === "notification" && event.data.kind === "textMention";
}
function isCustomNotificationEvent(event) {
    return event.type === "notification" && isCustomKind(event.data.kind);
}
;
(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$liveblocks$2f$core$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["detectDupes"])(PKG_NAME, PKG_VERSION, PKG_FORMAT);
;
}),
];

//# sourceMappingURL=node_modules_%40liveblocks_node_dist_index_15g8bvr.js.map